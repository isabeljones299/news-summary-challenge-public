export default class NewsItemModel {
    constructor(thumbnail, headline, bodyText, id) {
        this.thumbnail = thumbnail;
        this.headline = headline;
        this.bodyText = bodyText;
        this.id = id;
    }
}